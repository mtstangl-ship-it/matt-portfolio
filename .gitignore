node_modules
.next
out
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.DS_Store
tmp_py/
.v4-extract/

# Local reference snapshots (HTML exports for external design tools, not source)
reference/html-export/

# Verification artifacts — regenerated each gate run
reference/impact-visual-pass/verification-screenshots/
reference/impact-visual-pass/polish-pass-*-screenshots/

# Tooling cache (Playwright / local QA runs)
cache/

# One-time design handoffs (not source)
.handoff/

# Cursor session backup files
*.cursor-local.bak
